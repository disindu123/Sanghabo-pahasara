const nav=document.getElementById('nav');addEventListener('scroll',()=>nav.classList.toggle('scrolled',scrollY>35));
const menu=document.getElementById('menu'),mobile=document.getElementById('mobile'),close=document.getElementById('close'),overlay=document.getElementById('overlay');
function hide(){mobile.classList.remove('open');overlay.classList.remove('show')}menu.onclick=()=>{mobile.classList.add('open');overlay.classList.add('show')};close.onclick=hide;overlay.onclick=hide;document.querySelectorAll('.mobile a').forEach(a=>a.onclick=hide);
const ro=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');ro.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach((el,i)=>{el.style.transitionDelay=(i%5)*70+'ms';ro.observe(el)});
const counters=document.querySelectorAll('[data-count]');let started=false;const co=new IntersectionObserver(es=>{if(es[0].isIntersecting&&!started){started=true;counters.forEach(el=>{const t=+el.dataset.count,s=performance.now();function go(n){const p=Math.min((n-s)/1000,1);el.textContent=Math.floor(t*p)+'+';if(p<1)requestAnimationFrame(go)}requestAnimationFrame(go)});co.disconnect()}},{threshold:.5});if(counters[0])co.observe(counters[0]);
const form=document.getElementById('form'),success=document.getElementById('success');form.onsubmit=e=>{e.preventDefault();const d=Object.fromEntries(new FormData(form));const subject=encodeURIComponent('Media Unit Inquiry - '+(d.type||'General'));const body=encodeURIComponent(`Name: ${d.name}\nEmail: ${d.email}\nPhone: ${d.phone}\nOrganization: ${d.org||'N/A'}\nInquiry Type: ${d.type}\nEvent Date: ${d.date||'N/A'}\nPriority: ${d.priority}\n\nProject Details:\n${d.message}`);success.classList.add('show');window.location.href=`mailto:mediaunit@sanghabodhi.lk?subject=${subject}&body=${body}`;setTimeout(()=>{form.reset();setTimeout(()=>success.classList.remove('show'),4000)},500)};
